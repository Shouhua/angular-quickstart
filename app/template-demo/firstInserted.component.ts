import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'first-inserted',
    template: `
        <p>The first component!</p>
    `
})
export class FirstInsertedComponent {
}