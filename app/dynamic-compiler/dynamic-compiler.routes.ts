import { DynamicDetail } from './detail.component'
import { Routes } from '@angular/router';

export const DynamicCompilerRoutes : Routes = [
    {path: 'dynamiccompiler', component:DynamicDetail},
];